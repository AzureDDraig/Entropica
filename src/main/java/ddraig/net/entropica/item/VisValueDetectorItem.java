package ddraig.net.entropica.item;

import net.minecraft.world.item.Item;

public class VisValueDetectorItem extends Item {
    public VisValueDetectorItem(Properties properties) {
        super(properties);
    }
    // No special logic needed; the Renderer checks if the player is holding this.
}